---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json
modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        - lukenukem/asus-linux
      files:
        - https://pkgs.tailscale.com/stable/fedora/tailscale.repo
        - vscode.repo
      keys:
        - https://pkgs.tailscale.com/stable/fedora/repo.gpg
        - https://packages.microsoft.com/keys/microsoft.asc
    install:
      packages:
        - asusctl # Asus Power Profile management
        - btop # Resource monitor
        - chezmoi # Dotfile manager
        - code # VScode
        - edk2-ovmf # Virtualization
        - fastfetch # CLI system information
        - fish # Fish shell
        - gh # GitHub CLI
        - gparted # Partition editor
        - helix # Modal IDE
        - iotop # View IO stats
        - jetbrains-mono-fonts-all # JetBrains Mono font
        - kitty # Terminal emulator
        - libvirt # Virtualization
        - libvirt-nss
        - lm_sensors # Hardware sensor utility
        - nmtui # NetworkManager TUI
        - p7zip # 7zip
        - p7zip-plugins
        - playerctl # Media controller CLI
        - podman-compose # Compose for Podman
        - powertop # Power consumption and management CLI utility
        - qemu # Virtualisation
        - qemu-char-spice
        - qemu-device-display-virtio-gpu
        - qemu-device-display-virtio-vga
        - qemu-device-usb-redirect
        - qemu-img
        - qemu-system-x86-core
        - qemu-user-binfmt
        - qemu-user-static
        - rclone # Cloud storage CLI utility
        - rsms-inter-fonts # Inter font
        - tailscale # Mesh VPN
        - uv # Python package installer
        - uxplay # AirPlay receiver
        - virt-manager # Virtualisation
        - virt-v2v
        - virt-viewer
    remove:
      packages:
        - firefox # Replace with Flatpak browsers
        - firefox-langpacks
